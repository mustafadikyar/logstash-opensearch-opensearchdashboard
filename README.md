# opensearch-logstash-file-logging
Simple log management setup with OpenSearch, OpenSearch Dashboard and Logstash. It reads logs from files and indexes them into OpenSearch for searching and visualization.
